{
  "name": "higidus-site",
  "private": true,
  "version": "1.0.0",
  "description": "HIGIDUS multi-page website powered by Eleventy",
  "scripts": {
    "dev": "eleventy --serve --port=8080",
    "build": "eleventy",
    "clean": "rm -rf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
